#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🛡️ Sprawdzanie jakości kodu przed push..."
echo "🔍 Sprawdzam ESLint..."

# Run linting (ignore all exit codes - just informational)
pnpm -r lint >/dev/null 2>&1 || true

echo "⚠️ Sprawdzania kodu zakończone (ostrzeżenia są tolerowane)"
echo "💡 Push jest dozwolony - kontynuuję..."
