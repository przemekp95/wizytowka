# Page components

Shared page-level React 19 components for the Next.js 16 App Router. Run their
tests from the repository root with `corepack pnpm -F frontend test`.

Components must preserve keyboard navigation, visible focus, reduced-motion
behavior, and localized labels. Public forms submit only through the
same-origin route handlers in `src/app/api`.
